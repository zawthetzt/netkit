{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "/home/zack/Desktop/coding/netkit"],
      "description": "File system access for reading/writing project files"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub API access for repo operations, issues, and PRs"
    },
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "Web search for network documentation and troubleshooting"
    },
    "netkit": {
      "command": "netkit",
      "args": ["mcp"],
      "description": "Network diagnostics tools - ping, scan, dns, whois, trace, http, local, down"
    }
  }
}
